from setuptools import setup

setup(
    name="pib_hermes_config",
    version="0.1",
    description="shared filesystem locations of the Hermes agent used by pib",
    packages=["pib_hermes_config"],
)
