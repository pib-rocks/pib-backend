# Request
string chat_id
string text
bool   is_user
bool   update_database
string message_id    # pass "" to CREATE; non-empty to UPDATE

---
# Response
bool   successful
string message_id    # the effective PIB message id after the op
string content       # the full message content that was stored
