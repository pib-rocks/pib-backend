[pytest]
testpaths = integration infrastructure unit
pythonpath = ../pib_api/flask ../public_api_client
addopts = -ra
markers =
    docker: tests requiring a running Docker daemon and optionally built images
    slow: tests that start containers or wait for restarts
